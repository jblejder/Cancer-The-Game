<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="model"
            type="com.kutapps.keyten.home.HomeViewModel"/>

        <import type="com.kutapps.keyten.shared.constants.State"/>

        <import type="android.view.View"/>
    </data>

    <RelativeLayout
        android:id="@+id/activity_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:animateLayoutChanges="true"
            android:orientation="vertical"
            android:weightSum="1">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="0dp"
                android:layout_gravity="center"
                android:layout_weight="0.5"
                android:gravity="center"
                android:text="@{model.state != State.Keyten ? @string/keyten : @string/no_ten}"
                android:textAlignment="center"
                android:textSize="50sp"
                android:visibility="@{model.state != State.Init ? View.VISIBLE : View.GONE}"/>

        </LinearLayout>

        <Button
            android:id="@+id/send"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@color/colorPrimaryDark"
            android:paddingEnd="10dp"
            android:paddingStart="10dp"
            android:text="@string/keyten"
            android:textColor="@android:color/darker_gray"
            android:textSize="50sp"
            android:visibility="@{model.state == State.Init  ? View.GONE : View.VISIBLE}"/>

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:indeterminate="true"
            android:visibility="@{model.state == State.Init  ? View.VISIBLE : View.GONE}"/>

    </RelativeLayout>
</layout>